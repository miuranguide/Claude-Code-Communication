name: shared
description: Shared models, protocol, and utilities for TikTok Live OBS
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  json_annotation: ^4.8.1
  hive: ^2.2.3
  firebase_core: ^3.8.1
  cloud_firestore: ^5.6.0

dev_dependencies:
  json_serializable: ^6.7.1
  build_runner: ^2.4.6
  test: ^1.24.0
  lints: ^3.0.0
